# Supply chain security hardening
# Prevents arbitrary code execution during npm install
# If a legitimate package needs install scripts, use:
#   npm install --ignore-scripts=false <package>
# Or add to the allow list below.
ignore-scripts=true

# Enforce exact versions when adding new deps
save-exact=true

# Audit on install
audit=true
